# Задания к уроку

## Перед началом

Открой файл `student_template.py`. Пиши код под каждым заданием. Запускай программу и проверяй, что получается.

---

## Задание 1. Знакомство

Спроси у пользователя:
- имя
- возраст

Выведи одной строкой:

```
Привет, <имя>! Тебе <возраст> лет.
```

**Пример работы программы:**

```
Как тебя зовут? Аня
Сколько тебе лет? 12
Привет, Аня! Тебе 12 лет.
```

---

## Задание 2. Повторялка

Спроси у пользователя:
- слово
- число `n`

Выведи это слово `n` раз, каждый раз с новой строки.

**Пример:**

```
Введи слово: Python
Сколько раз повторить? 3
Python
Python
Python
```

---

## Задание 3. Линия из символов

Спроси у пользователя:
- символ (один знак, например `*` или `#`)
- число `n`

Нарисуй линию из этого символа длиной `n`.

**Пример:**

```
Введи символ: *
Сколько символов? 7
*******
```

> Подсказка: используй `print(symbol, end="")`, чтобы символы шли в одну строку.

---

## Задание 4. Нумерованное приветствие

Спроси у пользователя:
- имя
- число `n`

Выведи приветствие `n` раз с номером строки:

```
Как тебя зовут? Аня
Сколько раз поздороваться? 3
1. Привет, Аня!
2. Привет, Аня!
3. Привет, Аня!
```

> Подсказка: внутри цикла `for i in range(n)` переменная `i` принимает значения 0, 1, 2... Подумай, как получить 1, 2, 3.

---

## Задание 5. Мини-таблица умножения

Спроси у пользователя число.
Выведи таблицу умножения на это число от 1 до 5.

**Пример:**

```
Введи число: 3
3 x 1 = 3
3 x 2 = 6
3 x 3 = 9
3 x 4 = 12
3 x 5 = 15
```

> Подсказка: используй переменную цикла и `*` для умножения.

---

## Задание 6. Чётное или нечётное

Спроси у пользователя число.
Если оно чётное — выведи `Число чётное`.
Если нечётное — выведи `Число нечётное`.

**Пример 1:**

```
Введи число: 4
Число чётное
```

**Пример 2:**

```
Введи число: 7
Число нечётное
```

> Подсказка: число чётное, если остаток от деления на 2 равен нулю: `number % 2 == 0`.

---

## Задание 7. Можно ли получать права

Спроси у пользователя возраст.
Если возраст 16 или больше — выведи `Можно получать права`.
Иначе — выведи `Придётся подождать`.

**Пример 1:**

```
Сколько тебе лет? 18
Можно получать права
```

**Пример 2:**

```
Сколько тебе лет? 14
Придётся подождать
```

---

## Задание 8. Оценка по баллам

Спроси у пользователя количество баллов (от 0 до 100).
Выведи оценку по правилам:

| Баллы       | Оценка                |
|-------------|-----------------------|
| 90 и больше | Отлично               |
| 75–89       | Хорошо                |
| 60–74       | Удовлетворительно     |
| меньше 60   | Неудовлетворительно   |

**Пример:**

```
Сколько баллов? 82
Хорошо
```

> Подсказка: используй `if`, `elif` и `else`.

---

## Задание 9. Только чётные числа

Спроси у пользователя число `n`.
С помощью цикла `for` и условия `if` выведи только чётные числа от 1 до `n`.

**Пример:**

```
Введи число: 10
2
4
6
8
10
```

> Подсказка: перебери числа от 1 до n включительно и проверь каждое на чётность.

---

## Задание 10. Угадай секретное слово (со звёздочкой)

Загадай секретное слово, например `"python"`.
Позволь пользователю угадать его за 3 попытки.

Для каждой попытки:
- если слово угадано — выведи `Правильно!` и остановись;
- иначе — выведи `Неправильно, попробуй ещё раз`.

**Пример, если пользователь угадал со второй попытки:**

```
Угадай секретное слово: java
Неправильно, попробуй ещё раз
Угадай секретное слово: python
Правильно!
```

> Подсказка: используй `for` для трёх попыток и `if` для проверки слова. Если слово угадано, используй `break`, чтобы остановить цикл досрочно.

---

## Что проверить перед сдачей

- [ ] Программы запускаются без ошибок.
- [ ] Ввод работает правильно.
- [ ] Цикл повторяет нужное количество раз.
- [ ] В задании 3 линия не переносится на новую строку посередине.
- [ ] В задании 4 номера строк начинаются с 1, а не с 0.
- [ ] В заданиях 6–8 используются условия `if`, `else`, `elif`.
- [ ] В задании 9 совмещены цикл `for` и условие `if`.
- [ ] В задании 10 программа останавливается, если слово угадано.
